# ─────────────────────────────────────────────────────────────
# AI Image Generation Service v3.0 — Requirements
# Python 3.10+ recommended
# ─────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── PyTorch (install manually for CUDA) ───────────────────────
# CPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0,<2.3.0
torchvision>=0.16.0,<0.18.0

# ── Diffusion stack (stable combo) ────────────────────────────
diffusers==0.27.2
transformers==4.40.1
accelerate==0.29.3
safetensors==0.4.3
huggingface_hub==0.23.0

# ── Tokenizer + text processing (important) ───────────────────
ftfy>=6.1.0
sentencepiece>=0.2.0

# ── Math / schedulers support ─────────────────────────────────
scipy>=1.10.0
numpy>=1.24.0
einops>=0.7.0

# ── Image processing ──────────────────────────────────────────
Pillow==10.3.0

# ── Memory efficiency ─────────────────────────────────────────
# Install matching your CUDA + torch version
# Example:
# pip install xformers --index-url https://download.pytorch.org/whl/cu121
xformers>=0.0.25; sys_platform != "darwin"

# ─────────────────────────────────────────────────────────────
# Optional (ADVANCED — uncomment if needed)
# ─────────────────────────────────────────────────────────────

# Better performance (if available)
# triton>=2.1.0

# Debugging / profiling
# psutil>=5.9.0

# ControlNet (future expansion)
# controlnet_aux>=0.0.7
